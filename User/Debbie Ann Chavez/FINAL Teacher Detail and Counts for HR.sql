
/*******************************************************************************************************


DETAILS ON TOP QUERY, COUNTS IN SECOND QUERY BELOW

*********************************************************************************************************/

SELECT * FROM (
--STUDENTS IN HOMEROOM PERIOD 1 --DETAILS
SELECT 
	ROW_NUMBER() OVER (PARTITION BY ENROLL.SCHOOL_CODE, STF.BADGE_NUM ,BS.SIS_NUMBER ORDER BY SCH.PERIOD_BEGIN) AS RN
	,ENROLL.SCHOOL_CODE, SCHOOL_NAME, BS.LAST_NAME, BS.FIRST_NAME, BS.SIS_NUMBER, ENROLL.GRADE, BS.SPED_STATUS, BS.ELL_STATUS,  BS.RESOLVED_RACE
	,SCH.PERIOD_BEGIN, PERIOD_END, SCH.COURSE_ID, SCH.SECTION_ID, SCH.COURSE_TITLE
	,PERS.LAST_NAME AS TEACHER_LAST_NAME, PERS.FIRST_NAME AS TEACHER_FIRST_NAME, STF.BADGE_NUM
	,SCH2.VALUE_DESCRIPTION AS Staff_Responsibility
	,ADDSTAFF.[FIRST_NAME] AS ADD_STAFF_LAST_NAME, ADDSTAFF.[LAST_NAME] AS ADD_STAFF_FIRST_NAME
	,REPLACE(STAFF2.[BADGE_NUM],'e','') AS [ADD_STAFF_BADGE_NUM]
	,SCH.ORGANIZATION_GU
FROM 
	APS.ScheduleDetailsAsOf('20170814') AS SCH
	INNER JOIN 
	APS.BasicStudentWithMoreInfo AS BS
	ON
	SCH.STUDENT_GU = BS.STUDENT_GU
	INNER JOIN 
	APS.PrimaryEnrollmentDetailsAsOf('20170814') AS ENROLL
	ON
	ENROLL.STUDENT_GU = SCH.STUDENT_GU

	INNER JOIN 
	REV.REV_PERSON AS PERS
	ON
	PERS.PERSON_GU = SCH.STAFF_GU

	INNER JOIN
	REV.EPC_STAFF AS STF
	ON
	SCH.STAFF_GU = STF.STAFF_GU

	--ADDITIONAL STAFF
	LEFT JOIN	
	rev.EPC_SCH_YR_SECT_STF AS AdditionalStaff
	ON
	SCH.SECTION_GU = AdditionalStaff.SECTION_GU

	LEFT JOIN
	rev.EPC_STAFF_SCH_YR AS StaffSchoolYear
	ON
	AdditionalStaff.STAFF_SCHOOL_YEAR_GU = StaffSchoolYear.STAFF_SCHOOL_YEAR_GU

	LEFT JOIN 
	REV.EPC_STAFF AS STAFF2
	ON
	StaffSchoolYear.STAFF_GU = STAFF2.STAFF_GU

	LEFT JOIN 
	REV.REV_PERSON AS ADDSTAFF
	ON
	ADDSTAFF.PERSON_GU = StaffSchoolYear.STAFF_GU

	LEFT JOIN 
	APS.LookupTable('K12.ScheduleInfo','Staff_Responsibility') AS SCH2
	ON
	AdditionalStaFF.STAF_CONTR_RESPON = SCH2.VALUE_CODE

	WHERE
	--SCH.PERIOD_BEGIN = 1
	--AND 
	SCH.ORGANIZATION_NAME LIKE '%ELEMENTARY%'
	) AS T1

	WHERE RN = 1

	ORDER BY SCHOOL_CODE, BADGE_NUM



/*******************************************************************************************************


 COUNTS 

*********************************************************************************************************/



SELECT 
	SCHOOL_CODE, SCHOOL_NAME, BADGE_NUM, TEACHER_LAST_NAME, TEACHER_FIRST_NAME, GRADE,  COUNT(*) AS UNIQUE_STUDENT_COUNT_PER_TEACHER_PER_GRADE, SUM(COUNT_SPED) AS UNIQUE_SPED_COUNT, SUM(COUNT_ELL) AS UNIQUE_ELL_COUNT
	,ISNULL(REPLACE(REPLACE(MAX(HOMEROOM),1,'Y'),0,'N'),'') AS HOMEROOM
	,ISNULL(MAX(HOMEROOM_ADD_STAFF),'') AS ADD_STAFF_BADGE
	,ISNULL(MAX(HOMEROOM_ADD_LAST),'') AS ADD_STAFF_LAST_NAME
	,ISNULL(MAX(HOMEROOM_ADD_FIRST),'') AS ADD_STAFF_FIRST_NAME
	,ISNULL(MAX(HOMEROOM_ADD_RESP),'') AS ADD_STAFF_RESPONSIBILITY


 FROM (
--STUDENTS IN HOMEROOM PERIOD 1 --DETAILS
SELECT 
	ROW_NUMBER() OVER (PARTITION BY ENROLL.SCHOOL_CODE, STF.BADGE_NUM ,BS.SIS_NUMBER ORDER BY SCH.PERIOD_BEGIN) AS RN

	,ENROLL.SCHOOL_CODE, SCHOOL_NAME, BS.LAST_NAME, BS.FIRST_NAME, BS.SIS_NUMBER, ENROLL.GRADE, BS.SPED_STATUS, BS.ELL_STATUS,  BS.RESOLVED_RACE
	,SCH.PERIOD_BEGIN, PERIOD_END, SCH.COURSE_ID, SCH.SECTION_ID, SCH.COURSE_TITLE
	,PERS.LAST_NAME AS TEACHER_LAST_NAME, PERS.FIRST_NAME AS TEACHER_FIRST_NAME, STF.BADGE_NUM

	,SCH2.VALUE_DESCRIPTION AS Staff_Responsibility
		,ADDSTAFF.[FIRST_NAME] AS ADD_STAFF_LAST_NAME, ADDSTAFF.[LAST_NAME] AS ADD_STAFF_FIRST_NAME
	,REPLACE(STAFF2.[BADGE_NUM],'e','') AS [ADD_STAFF_BADGE_NUM]
	, CASE WHEN SPED_STATUS = 'Y' THEN 1 ELSE 0 END AS COUNT_SPED
	, CASE WHEN ELL_STATUS = 'Y' THEN 1 ELSE 0 END AS COUNT_ELL
	, CASE WHEN SCH.PERIOD_BEGIN = 1 THEN 1 ELSE 0 END AS HOMEROOM
	,CASE WHEN SCH.PERIOD_BEGIN = 1 THEN REPLACE(STAFF2.[BADGE_NUM],'e','') ELSE '' END AS HOMEROOM_ADD_STAFF
	,CASE WHEN SCH.PERIOD_BEGIN = 1 THEN ADDSTAFF.LAST_NAME ELSE '' END AS HOMEROOM_ADD_LAST
	,CASE WHEN SCH.PERIOD_BEGIN = 1 THEN ADDSTAFF.[FIRST_NAME]ELSE '' END AS HOMEROOM_ADD_FIRST
	,CASE WHEN SCH.PERIOD_BEGIN = 1 THEN SCH2.VALUE_DESCRIPTION ELSE '' END AS HOMEROOM_ADD_RESP

FROM 
	APS.ScheduleDetailsAsOf('20170814') AS SCH
	INNER JOIN 
	APS.BasicStudentWithMoreInfo AS BS
	ON
	SCH.STUDENT_GU = BS.STUDENT_GU
	INNER JOIN 
	APS.PrimaryEnrollmentDetailsAsOf('20170814') AS ENROLL
	ON
	ENROLL.STUDENT_GU = SCH.STUDENT_GU

	INNER JOIN 
	REV.REV_PERSON AS PERS
	ON
	PERS.PERSON_GU = SCH.STAFF_GU

	INNER JOIN
	REV.EPC_STAFF AS STF
	ON
	SCH.STAFF_GU = STF.STAFF_GU

	--ADDITIONAL STAFF
	LEFT JOIN	
	rev.EPC_SCH_YR_SECT_STF AS AdditionalStaff
	ON
	SCH.SECTION_GU = AdditionalStaff.SECTION_GU

	LEFT JOIN
	rev.EPC_STAFF_SCH_YR AS StaffSchoolYear
	ON
	AdditionalStaff.STAFF_SCHOOL_YEAR_GU = StaffSchoolYear.STAFF_SCHOOL_YEAR_GU

	LEFT JOIN 
	REV.EPC_STAFF AS STAFF2
	ON
	StaffSchoolYear.STAFF_GU = STAFF2.STAFF_GU

	LEFT JOIN 
	REV.REV_PERSON AS ADDSTAFF
	ON
	ADDSTAFF.PERSON_GU = StaffSchoolYear.STAFF_GU

	LEFT JOIN 
	APS.LookupTable('K12.ScheduleInfo','Staff_Responsibility') AS SCH2
	ON
	AdditionalStaFF.STAF_CONTR_RESPON = SCH2.VALUE_CODE

	WHERE
	--SCH.PERIOD_BEGIN = 1
	--AND 
	SCH.ORGANIZATION_NAME LIKE '%ELEMENTARY%'
	) AS T1

	WHERE RN = 1

	GROUP BY SCHOOL_CODE, SCHOOL_NAME, BADGE_NUM, TEACHER_LAST_NAME, TEACHER_FIRST_NAME, GRADE

	ORDER BY SCHOOL_CODE, SCHOOL_NAME, TEACHER_LAST_NAME, TEACHER_FIRST_NAME, GRADE